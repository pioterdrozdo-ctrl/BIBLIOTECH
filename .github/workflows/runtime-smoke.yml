name: Runtime smoke check

on:
  pull_request:
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'package.json'
      - 'package-lock.json'
      - 'scripts/runtime-smoke.js'
      - 'scripts/account-ui-smoke.js'
      - 'scripts/isbn-metadata-ui-smoke.js'
      - 'scripts/isbn-metadata-api-smoke.js'
      - 'scripts/reservation-queue-ui-smoke.js'
      - 'scripts/reservation-queue-api-smoke.js'
      - 'scripts/book-import-ui-smoke.js'
      - 'scripts/book-import-api-smoke.js'
      - 'scripts/account-api-smoke.js'
      - '.github/workflows/runtime-smoke.yml'
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'package.json'
      - 'package-lock.json'
      - 'scripts/runtime-smoke.js'
      - 'scripts/account-ui-smoke.js'
      - 'scripts/isbn-metadata-ui-smoke.js'
      - 'scripts/isbn-metadata-api-smoke.js'
      - 'scripts/reservation-queue-ui-smoke.js'
      - 'scripts/reservation-queue-api-smoke.js'
      - 'scripts/book-import-ui-smoke.js'
      - 'scripts/book-import-api-smoke.js'
      - 'scripts/account-api-smoke.js'

permissions:
  contents: read

jobs:
  runtime-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 32
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install application
        run: npm install
      - name: Install browser test runtime
        run: |
          npm install --no-save playwright@1.52.0
          npx playwright install --with-deps chromium
      - name: Start server
        env:
          NODE_ENV: production
          PORT: 4173
          JWT_SECRET: runtime-smoke-secret
        run: |
          mkdir -p /tmp/runtime-artifacts
          node backend/server.js > /tmp/runtime-artifacts/bibliotech-server.log 2>&1 &
          echo $! > /tmp/bibliotech-server.pid
          for attempt in $(seq 1 40); do
            if curl --fail --silent http://127.0.0.1:4173/api/health > /tmp/runtime-artifacts/health.json; then
              cat /tmp/runtime-artifacts/health.json
              exit 0
            fi
            sleep 1
          done
          cat /tmp/runtime-artifacts/bibliotech-server.log
          exit 1
      - name: Test real pages in Chromium
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/runtime-smoke.js 2>&1 | tee /tmp/runtime-artifacts/runtime-smoke.log
      - name: Test ISBN metadata in Chromium
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/isbn-metadata-ui-smoke.js 2>&1 | tee /tmp/runtime-artifacts/isbn-metadata-ui-smoke.log
      - name: Test ISBN metadata APIs
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/isbn-metadata-api-smoke.js 2>&1 | tee /tmp/runtime-artifacts/isbn-metadata-api-smoke.log
      - name: Test reservation queue APIs
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/reservation-queue-api-smoke.js 2>&1 | tee /tmp/runtime-artifacts/reservation-queue-api-smoke.log
      - name: Test reservation queue in Chromium
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/reservation-queue-ui-smoke.js 2>&1 | tee /tmp/runtime-artifacts/reservation-queue-ui-smoke.log
      - name: Test complete account settings in Chromium
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/account-ui-smoke.js 2>&1 | tee /tmp/runtime-artifacts/account-ui-smoke.log
      - name: Test bulk book import in Chromium
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/book-import-ui-smoke.js 2>&1 | tee /tmp/runtime-artifacts/book-import-ui-smoke.log
      - name: Test bulk book import APIs
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/book-import-api-smoke.js 2>&1 | tee /tmp/runtime-artifacts/book-import-api-smoke.log
      - name: Test account APIs and session revocation
        env:
          SMOKE_BASE_URL: http://127.0.0.1:4173
        run: |
          set -o pipefail
          node scripts/account-api-smoke.js 2>&1 | tee /tmp/runtime-artifacts/account-api-smoke.log
      - name: Upload runtime diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: runtime-diagnostics
          path: /tmp/runtime-artifacts
          if-no-files-found: warn
          retention-days: 3
      - name: Print server log
        if: always()
        run: cat /tmp/runtime-artifacts/bibliotech-server.log || true
      - name: Stop server
        if: always()
        run: test -f /tmp/bibliotech-server.pid && kill $(cat /tmp/bibliotech-server.pid) || true
