{
  "name": "bibliotech-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:init": "psql -U postgres -d bibliotech -f sql/init.sql",
    "health": "node -e \"fetch('http://localhost:'+(process.env.PORT||3001)+'/api/health').then(r=>r.json()).then(console.log)\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "pg": "^8.21.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
