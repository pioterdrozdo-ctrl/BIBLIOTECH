{
  "name": "bibliotech-ui-site",
  "version": "1.0.0",
  "private": true,
  "description": "BIBLIOTECH library catalog served by one Express backend.",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "NODE_ENV=development node backend/server.js",
    "health": "node -e \"fetch('http://localhost:'+(process.env.PORT||3001)+'/api/health').then(r=>r.json()).then(console.log)\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "pg": "^8.21.0"
  }
}
